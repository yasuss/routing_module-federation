{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @routing_react-router/* --parallel start",
    "build": "lerna run --scope @routing_react-router/* build",
    "serve": "lerna run --scope @routing_react-router/* --parallel serve",
    "clean": "lerna run --scope @routing_react-router/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "name": "routing_react-router"
}
