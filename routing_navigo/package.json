{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @routing_navigo/* --parallel start",
    "build": "lerna run --scope @routing_navigo/* build",
    "serve": "lerna run --scope @routing_navigo/* --parallel serve",
    "clean": "lerna run --scope @routing_navigo/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "name": "routing_navigo"
}
