import { Router, Route, Options } from './types/router';
import { RouteNode } from 'route-node';
declare const createRouter: <Dependencies extends Record<string, any> = Record<string, any>>(routes?: Route<Dependencies>[] | RouteNode, options?: Partial<Options>, dependencies?: Dependencies) => Router<Dependencies>;
export default createRouter;
