{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @routing_router5/* --parallel start",
    "build": "lerna run --scope @routing_router5/* build",
    "serve": "lerna run --scope @routing_router5/* --parallel serve",
    "clean": "lerna run --scope @routing_router5/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "name": "routing_router5"
}
