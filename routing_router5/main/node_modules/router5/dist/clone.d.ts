import { Router, DefaultDependencies } from './types/router';
export default function cloneRouter<Dependencies extends DefaultDependencies = DefaultDependencies>(router: Router, dependencies?: Dependencies): Router<Dependencies>;
